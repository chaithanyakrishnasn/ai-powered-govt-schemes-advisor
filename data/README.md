# Data

| Directory   | Contents                                      |
|-------------|-----------------------------------------------|
| `raw/`      | Raw scraped HTML/JSON dumps from portals      |
| `processed/`| Cleaned + structured JSON ready for ingestion |

Both directories are git-ignored except for `.gitkeep` sentinels.
